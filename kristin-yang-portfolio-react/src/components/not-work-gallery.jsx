import { playProjects } from '@/lib/play-projects';
import styles from './not-work-gallery.module.css';
export default function NotWorkGallery() { return <section className={`${styles.gallery} py-20`} aria-label="Personal projects gallery">{playProjects.map((project, i) => <a href={`/not-work/${project.slug}`} key={project.slug} className={`${styles.card} ${styles[project.slug]}`}><div className={styles.visual}><img src={project.cover} alt={project.title === 'HBD' ? 'An animated birthday card from HBD' : `${project.title} — original project artwork`} loading={i < 2 ? 'eager' : 'lazy'}/><span className={styles.open} aria-hidden="true">↗</span></div><div className={styles.heading}><h2 className="text-xl font-medium tracking-tight text-neutral-900">{project.title}</h2><span>{String(i + 1).padStart(2, '0')}</span></div><div className={styles.caption}><p>{project.slug === 'hbd' ? 'Digital celebrations sent from London to loved ones back home.' : project.slug === 'how-to-at-home' ? 'Reimagining what a home can be during pandemic quarantine.' : project.intro}</p><span className="text-[10px] font-mono tracking-widest text-neutral-400 uppercase">{project.tools?.join(' · ')}</span></div></a>)}</section>; }
